EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Do not put the service role key here — it must never ship inside the mobile app.
# For backends (e.g. Railway), use a separate server-only env file (see supabase/.env.example).
